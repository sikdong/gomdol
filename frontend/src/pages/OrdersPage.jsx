import React from 'react';

const OrdersPage = () => (
  <section>
    <h2>Order History</h2>
    <p>Once the backend endpoints are ready, your completed orders will show here.</p>
  </section>
);

export default OrdersPage;
